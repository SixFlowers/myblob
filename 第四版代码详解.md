# 第四版代码详解：异步任务版

> **版本**: 4.0  
> **日期**: 2026-04-02  
> **目标**: 实现异步任务架构，对标 AnyBlob 的 Transaction 和 MessageResult  
> **基于**: 第三版（连接池版）

---

## 📋 一、版本概述

### 1.1 本版目标

从**同步调用**升级到**异步任务模式**，实现：
- 请求/响应的解耦
- 支持回调机制
- 支持多文件并发下载
- 支持 PUT/DELETE 操作
- 支持大文件分片上传

### 1.2 核心概念：同步 vs 异步

**同步模式（第三版）**：
```cpp
// 用户调用 → 阻塞等待 → 返回结果
auto response = provider->download("file.txt");
process(response);  // 必须等待 download 完成
```

**异步模式（第四版）**：
```cpp
// 用户调用 → 立即返回 → 任务入队 → 后台执行 → 回调通知
provider->downloadAsync("file.txt", [](MessageResult& result) {
    process(result);  // 下载完成后自动调用
});
// 主线程可以继续做其他事情
```

### 1.3 架构变化

```
第三版（同步）：
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Provider  │───▶│ HttpClient  │───▶│ConnectionMgr│
└─────────────┘    └─────────────┘    └─────────────┘
       │                                    │
       ▼                                    ▼
   阻塞等待                           返回结果

第四版（异步）：
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│ Transaction │───▶│   Message   │───▶│   Message   │
│  (请求封装) │    │   Result    │    │   Task      │
└─────────────┘    │  (结果容器)  │    │  (任务执行) │
       │           └─────────────┘    └─────────────┘
       ▼                   ▲                  │
┌─────────────┐            │                  │
│  回调函数   │────────────┴──────────────────┘
│ (完成通知)  │     下载完成后自动调用
└─────────────┘
```

---

## 📁 二、新增/修改文件

### 2.1 新增文件列表

```
第四版新增文件
├── include/
│   ├── network/
│   │   ├── http_helper.hpp        # HTTP 响应解析工具
│   │   ├── message_result.hpp     # 异步结果容器
│   │   ├── original_message.hpp   # 原始请求封装
│   │   └── message_failure_code.hpp # 失败错误码
│   └── cloud/
│       └── transaction.hpp        # 事务管理器
│
├── src/
│   ├── network/
│   │   ├── http_helper.cpp       # HTTP 响应解析实现
│   │   ├── message_result.cpp     # 结果实现
│   │   └── original_message.cpp  # 原始消息实现
│   └── cloud/
│       └── transaction.cpp        # 事务实现
│
└── 第四版代码详解.md                # 本文档
```

### 2.2 修改文件列表

```
需要修改的文件
├── include/
│   ├── cloud/
│   │   ├── provider.hpp            # 添加异步方法
│   │   └── http_provider.hpp       # 实现异步方法
│   └── network/
│       ├── http_request.hpp        # 添加 Method 枚举
│       └── http_response.hpp        # 添加 Code 枚举
│
└── src/
    ├── cloud/
    │   ├── provider.cpp            # 实现 Transaction
    │   └── http_provider.cpp       # 实现异步请求
    └── network/
        └── http_response.cpp       # 实现辅助方法
```

---

## 🔍 三、类型定义验证（对照代码审查清单）

### 3.1 必须保持一致的类型

| 类型 | AnyBlob 真实定义 | MyBlob 定义 | 状态 |
|------|----------------|-------------|------|
| `CloudService` | `enum class : uint8_t` | `enum class : uint8_t` | ✅ |
| `HttpRequest::Method` | `enum class : uint8_t` | **需添加** | ❌ |
| `HttpResponse::Code` | `enum class : uint8_t` | **需添加** | ❌ |
| `HttpRequest.headers` | `map<string,string>` | `map<string,string>` | ✅ |
| `HttpResponse.body` | **无此字段** | 需修正 | ❌ |

### 3.2 新增的 AnyBlob 类型

| 类型 | 说明 | 需要实现 |
|------|------|---------|
| `MessageState` | 任务状态枚举 | ✅ |
| `MessageFailureCode` | 失败错误码枚举 | ✅ |
| `HttpHelper` | HTTP 响应解析工具 | ✅ |
| `HttpHelper::Info` | HTTP 响应信息 | ✅ |
| `MessageResult` | 结果容器 | ✅ |
| `OriginalMessage` | 原始请求封装 | ✅ |
| `Transaction` | 事务管理器 | ✅ |

---

## 📝 四、新增头文件详解

### 4.1 message_state.hpp - 任务状态枚举

> 📁 文件：`include/network/message_state.hpp`（**新建此文件**）

```cpp
// ============================================================================
// message_state.hpp - 异步任务状态枚举
// ============================================================================
// 任务从创建到完成的生命周期状态
// ============================================================================

#pragma once

namespace myblob::network {

/// 异步任务状态机
enum class MessageState : uint8_t {
    Init,            // 初始化
    TLSHandshake,    // TLS 握手中
    InitSending,     // 开始发送
    Sending,          // 发送中
    InitReceiving,    // 开始接收
    Receiving,        // 接收中
    TLSShutdown,     // TLS 关闭中
    Finished,        // 完成
    Aborted,         // 被中止
    Cancelled        // 被取消
};

}  // namespace myblob::network
```

**状态流转图**：
```
Init ──▶ TLSHandshake ──▶ InitSending ──▶ Sending ──▶ InitReceiving
  │           │                │                          │
  │           ▼                ▼                          ▼
  │      Aborted/         Finished/                    Finished
  │      Cancelled        Aborted/
                        Cancelled
  │
  └──────────────────────────────────────────────────▶ Finished
```

### 4.2 MessageFailureCode - 失败错误码枚举

> 📁 文件：`include/network/message_failure_code.hpp`（**新建此文件**）

```cpp
// ============================================================================
// message_failure_code.hpp - 失败错误码枚举
// ============================================================================
// 功能: 定义异步任务可能的失败原因，用于精确定位错误
// ============================================================================

#pragma once

namespace myblob::network {

/// 失败错误码（使用位掩码，支持组合）
enum class MessageFailureCode : uint16_t {
    None = 0,              // 无错误
    Socket = 1,            // Socket 创建错误
    Empty = 1 << 1,        // 空请求错误
    Timeout = 1 << 2,      // 超时
    Send = 1 << 3,         // Send 系统调用错误
    Recv = 1 << 4,         // Recv 系统调用错误
    HTTP = 1 << 5,         // HTTP 协议错误
    TLS = 1 << 6           // TLS 错误
};

}  // namespace myblob::network
```

### 4.3 http_helper.hpp - HTTP 响应解析工具

> 📁 文件：`include/network/http_helper.hpp`（**新建此文件**）

```cpp
// ============================================================================
// http_helper.hpp - HTTP 响应解析工具
// ============================================================================
// AnyBlob 对应: http_helper.hpp
// 功能: 解析 HTTP 响应，检测传输编码，判断接收是否完成
// ============================================================================

#pragma once
#include "http_response.hpp"
#include <cstdint>
#include <memory>
#include <string_view>

namespace myblob::network {

class HttpHelper {
public:
    /// HTTP 传输编码类型
    enum class Encoding : uint8_t {
        Unknown,           // 未知
        ContentLength,     // Content-Length 编码
        ChunkedEncoding    // 分块传输编码
    };

    /// HTTP 响应信息结构体
    struct Info {
        HttpResponse response;   // HTTP 响应头
        uint64_t length{0};      // 内容长度
        uint32_t headerLength{0}; // 头部长度
        Encoding encoding{Encoding::Unknown}; // 编码类型
    };

    /// 从原始数据中提取 HTTP 内容（会修改 data）
    static std::string_view retrieveContent(
        const uint8_t* data,
        uint64_t length,
        std::unique_ptr<Info>& info
    );

    /// 检测 HTTP 响应是否接收完成
    static bool finished(
        const uint8_t* data,
        uint64_t length,
        std::unique_ptr<Info>& info
    );

private:
    /// 检测协议类型（私有静态方法）
    static Info detect(std::string_view header);
};

}  // namespace myblob::network
```

### 4.4 http_helper.cpp - HTTP 响应解析实现

> 📁 文件：`src/network/http_helper.cpp`（**新建此文件**）

```cpp
// ============================================================================
// http_helper.cpp - HTTP 响应解析实现
// ============================================================================

#include "network/http_helper.hpp"
#include <cassert>
#include <charconv>
#include <cstring>
#include <stdexcept>
#include <string>

namespace myblob::network {

HttpHelper::Info HttpHelper::detect(std::string_view header) {
    Info info;
    info.response = HttpResponse::deserialize(header);

    static constexpr std::string_view transferEncoding = "Transfer-Encoding";
    static constexpr std::string_view chunkedEncoding = "chunked";
    static constexpr std::string_view contentLength = "Content-Length";
    static constexpr std::string_view headerEnd = "\r\n\r\n";

    info.encoding = Encoding::Unknown;
    for (auto& keyValue : info.response.headers) {
        if (transferEncoding == keyValue.first && chunkedEncoding == keyValue.second) {
            info.encoding = Encoding::ChunkedEncoding;
            auto end = header.find(headerEnd);
            info.headerLength = static_cast<uint32_t>(end + headerEnd.length());
        } else if (contentLength == keyValue.first) {
            info.encoding = Encoding::ContentLength;
            from_chars(keyValue.second.data(),
                      keyValue.second.data() + keyValue.second.size(),
                      info.length);
            auto end = header.find(headerEnd);
            info.headerLength = static_cast<uint32_t>(end + headerEnd.length());
        }
    }

    if (info.encoding == Encoding::Unknown && !HttpResponse::withoutContent(info.response.code))
        throw std::runtime_error("Unsupported HTTP encoding protocol");

    if (HttpResponse::withoutContent(info.response.code))
        info.headerLength = static_cast<uint32_t>(header.length());

    return info;
}

std::string_view HttpHelper::retrieveContent(
    const uint8_t* data,
    uint64_t length,
    std::unique_ptr<Info>& info
) {
    std::string_view sv(reinterpret_cast<const char*>(data), length);
    if (!info)
        info = std::make_unique<Info>(detect(sv));

    if (info->encoding == Encoding::ContentLength) {
        return std::string_view(
            reinterpret_cast<const char*>(data) + info->headerLength,
            info->length
        );
    }
    return {};
}

bool HttpHelper::finished(
    const uint8_t* data,
    uint64_t length,
    std::unique_ptr<Info>& info
) {
    if (!info) {
        std::string_view sv(reinterpret_cast<const char*>(data), length);
        info = std::make_unique<Info>(detect(sv));
    }

    if (info && HttpResponse::withoutContent(info->response.code))
        return true;

    switch (info->encoding) {
        case Encoding::ContentLength:
            return length >= info->headerLength + info->length;

        case Encoding::ChunkedEncoding: {
            std::string_view sv(reinterpret_cast<const char*>(data), length);
            info->length = sv.find("0\r\n\r\n"sv);
            bool ret = info->length != sv.npos;
            if (ret)
                info->length -= info->headerLength;
            return ret;
        }

        default: {
            info = nullptr;
            throw std::runtime_error("Unsupported HTTP transfer protocol");
        }
    }
}

}  // namespace myblob::network
```

**HttpHelper 核心功能**：

| 方法 | 作用 |
|------|------|
| `detect()` | 解析 HTTP 头，检测编码类型 |
| `retrieveContent()` | 从原始数据中提取 HTTP 内容 |
| `finished()` | 判断 HTTP 响应是否接收完成 |

**HTTP 传输编码类型**：

| 编码类型 | 说明 | 如何判断结束 |
|----------|------|-------------|
| `ContentLength` | 固定长度 | 收到 `Content-Length` 指定字节数 |
| `ChunkedEncoding` | 分块传输 | 收到 `0\r\n\r\n` 结束块 |
| `Unknown` | 未知/无内容 | 如 204 No Content |

### 4.5 message_result.hpp - 异步结果容器

> 📁 文件：`include/network/message_result.hpp`（**新建此文件**）

```cpp
// ============================================================================
// message_result.hpp - 异步任务结果容器
// ============================================================================
// AnyBlob 对应: message_result.hpp
// 功能: 存储异步任务的执行结果，包括响应数据和错误信息
// ============================================================================

#pragma once
#include "../utils/data_vector.hpp"
#include "http_helper.hpp"
#include "message_state.hpp"
#include <atomic>
#include <memory>
#include <string_view>

namespace myblob::network {

struct OriginalMessage;
class Transaction;

class MessageResult {
public:
    MessageResult();
    
    explicit MessageResult(uint8_t* data, uint64_t size);
    
    explicit MessageResult(utils::DataVector<uint8_t>* dataVector);
    
    [[nodiscard]] std::string_view getResult() const;
    [[nodiscard]] std::string_view getResult();
    [[nodiscard]] const uint8_t* getData() const;
    [[nodiscard]] uint8_t* getData();
    [[nodiscard]] std::unique_ptr<uint8_t[]> moveData();
    [[nodiscard]] uint64_t getSize() const;
    [[nodiscard]] uint64_t getOffset() const;
    [[nodiscard]] MessageState getState() const;
    [[nodiscard]] uint16_t getFailureCode() const;
    [[nodiscard]] std::string_view getErrorResponse() const;
    [[nodiscard]] std::string_view getResponseCode() const;
    [[nodiscard]] uint64_t getResponseCodeNumber() const;
    [[nodiscard]] bool owned() const;
    [[nodiscard]] bool success() const;
    [[nodiscard]] utils::DataVector<uint8_t>& getDataVector();
    [[nodiscard]] std::unique_ptr<utils::DataVector<uint8_t>> moveDataVector();

protected:
    std::unique_ptr<utils::DataVector<uint8_t>> dataVector_;
    std::unique_ptr<HttpHelper::Info> response_;
    const MessageResult* originError_{nullptr};
    uint16_t failureCode_{0};
    std::atomic<MessageState> state_{MessageState::Init};
    
    friend OriginalMessage;
    friend Transaction;
    friend HTTPMessage;
    friend HTTPSMessage;
    friend TLSConnection;
};

}  // namespace myblob::network
```

**核心设计**：

1. **数据存储**：使用 `DataVector<uint8_t>` 而非 `std::string`
2. **HTTP 响应信息**：使用 `HttpHelper::Info` 存储响应头、编码类型等
3. **状态追踪**：`std::atomic<MessageState>` 支持线程安全的状态查询
4. **失败码**：`MessageFailureCode` 支持多种失败类型（超时、连接失败、HTTP 错误等）
5. **错误传播**：`originError_` 用于错误响应链

**关键方法**：

| 方法 | 说明 |
|------|------|
| `getResult()` | 获取响应体数据 |
| `getData()` | 获取原始数据指针 |
| `moveData()` | 转移数据所有权 |
| `getState()` | 获取当前状态 |
| `success()` | 判断是否成功 |
| `getFailureCode()` | 获取失败错误码 |
| `getResponseCodeNumber()` | 获取 HTTP 状态码数字 |

### 4.6 original_message.hpp - 原始请求封装

> 📁 文件：`include/network/original_message.hpp`（**新建此文件**）

```cpp
// ============================================================================
// original_message.hpp - 原始请求消息封装
// ============================================================================
// AnyBlob 对应: original_message.hpp
// 功能: 封装原始 HTTP 请求，包含请求内容、提供者信息和结果容器
// ============================================================================

#pragma once
#include "../utils/data_vector.hpp"
#include "message_result.hpp"
#include <cstdint>
#include <memory>

namespace myblob::cloud {
class Provider;
}  // namespace myblob::cloud

namespace myblob::network {

struct OriginalMessage {
    std::unique_ptr<utils::DataVector<uint8_t>> message;
    cloud::Provider& provider;
    MessageResult result;
    
    const uint8_t* putData{nullptr};
    uint64_t putLength{0};
    uint64_t traceId{0};
    
    OriginalMessage(
        std::unique_ptr<utils::DataVector<uint8_t>> msg,
        cloud::Provider& provider,
        uint8_t* receiveBuffer = nullptr,
        uint64_t bufferSize = 0,
        uint64_t trace = 0
    );
    
    virtual ~OriginalMessage() = default;
    
    virtual bool requiresFinish() { return false; }
    
    virtual void finish() {}
    
    void setPutRequestData(const uint8_t* data, uint64_t length) {
        this->putData = data;
        this->putLength = length;
    }
    
    void setResultVector(utils::DataVector<uint8_t>* dataVector) {
        result.dataVector_ = std::unique_ptr<utils::DataVector<uint8_t>>(dataVector);
    }
};

template <typename Callback>
struct OriginalCallbackMessage : public OriginalMessage {
    Callback callback;
    
    OriginalCallbackMessage(
        Callback&& cb,
        std::unique_ptr<utils::DataVector<uint8_t>> msg,
        cloud::Provider& provider,
        uint8_t* receiveBuffer = nullptr,
        uint64_t bufferSize = 0,
        uint64_t trace = 0
    ) : OriginalMessage(std::move(msg), provider, receiveBuffer, bufferSize, trace),
        callback(std::forward<Callback>(cb)) {}
    
    bool requiresFinish() override { return true; }
    
    void finish() override {
        callback(result);
    }
};

}  // namespace myblob::network
```

**关键特性**：

1. **模板化回调**：`OriginalCallbackMessage<Callback>` 支持任意回调函数
2. **PUT 请求支持**：通过 `putData` 和 `putLength` 支持上传
3. **RAII**：通过 `unique_ptr` 自动管理内存

### 4.7 transaction.hpp - 事务管理器

> 📁 文件：`include/cloud/transaction.hpp`（**新建此文件**）

```cpp
// ============================================================================
// transaction.hpp - 事务管理器
// ============================================================================
// AnyBlob 对应: transaction.hpp
// 功能: 高级请求封装，支持 GET/PUT/DELETE，自动处理范围请求
// ============================================================================

#pragma once
#include "../network/message_result.hpp"
#include "../network/original_message.hpp"
#include "provider.hpp"
#include <memory>
#include <vector>

namespace myblob::cloud {

class Transaction {
public:
    using message_vector_type = std::vector<std::unique_ptr<network::OriginalMessage>>;
    
    Transaction();
    
    explicit Transaction(Provider* provider);
    
    void setProvider(Provider* provider);
    
    bool getObjectRequest(
        const std::string& remotePath,//下载路径
        std::pair<uint64_t, uint64_t> range = {0, 0},//数据范围
        uint8_t* result = nullptr,//接收数据缓冲区
        uint64_t capacity = 0//缓冲区大小
    );
    
    template <typename Callback>
    bool getObjectRequest(
        Callback&& callback,
        const std::string& remotePath,
        std::pair<uint64_t, uint64_t> range = {0, 0},
        uint8_t* result = nullptr,
        uint64_t capacity = 0
    );
    
    bool putObjectRequest(
        const std::string& remotePath,
        const char* data,
        uint64_t size,
        uint8_t* result = nullptr,
        uint64_t capacity = 0
    );
    
    template <typename Callback>
    bool putObjectRequest(
        Callback&& callback,
        const std::string& remotePath,
        const char* data,
        uint64_t size,
        uint8_t* result = nullptr,
        uint64_t capacity = 0
    );
    
    bool deleteObjectRequest(
        const std::string& remotePath,
        uint8_t* result = nullptr,
        uint64_t capacity = 0
    );
    
    template <typename Callback>
    bool deleteObjectRequest(
        Callback&& callback,
        const std::string& remotePath,
        uint8_t* result = nullptr,
        uint64_t capacity = 0
    );
    
    message_vector_type& getMessages() { return messages_; }
    
    const message_vector_type& getMessages() const { return messages_; }

private:
    Provider* provider_{nullptr};
    message_vector_type messages_;
};

template <typename Callback>
bool Transaction::getObjectRequest(
    Callback&& callback,
    const std::string& remotePath,
    std::pair<uint64_t, uint64_t> range,
    uint8_t* result,
    uint64_t capacity
) {
    if (!provider_) return false;
    
    auto msg = std::make_unique<network::OriginalCallbackMessage<Callback>>(
        std::forward<Callback>(callback),
        provider_->getRequest(remotePath, range),
        *provider_,
        result,
        capacity
    );
    
    messages_.push_back(std::move(msg));
    return true;
}

template <typename Callback>
bool Transaction::putObjectRequest(
    Callback&& callback,
    const std::string& remotePath,
    const char* data,
    uint64_t size,
    uint8_t* result,
    uint64_t capacity
) {
    if (!provider_) return false;
    
    auto object = std::string_view(data, size);
    auto msg = std::make_unique<network::OriginalCallbackMessage<Callback>>(
        std::forward<Callback>(callback),
        provider_->putRequest(remotePath, object),
        *provider_,
        result,
        capacity
    );
    
    msg->setPutRequestData(reinterpret_cast<const uint8_t*>(data), size);
    messages_.push_back(std::move(msg));
    return true;
}

template <typename Callback>
bool Transaction::deleteObjectRequest(
    Callback&& callback,
    const std::string& remotePath,
    uint8_t* result,
    uint64_t capacity
) {
    if (!provider_) return false;
    
    auto msg = std::make_unique<network::OriginalCallbackMessage<Callback>>(
        std::forward<Callback>(callback),
        provider_->deleteRequest(remotePath),
        *provider_,
        result,
        capacity
    );
    
    messages_.push_back(std::move(msg));
    return true;
}

}  // namespace myblob::cloud
```

---

## ✏️ 五、修改文件详解

### 5.1 http_request.hpp - 添加 Method 枚举

> 📁 文件：`include/network/http_request.hpp`（**需要修改**）

**当前问题**：缺少 `Method` 枚举

**修改后**：

```cpp
#pragma once
#include "../utils/data_vector.hpp"
#include <cstdint>
#include <map>
#include <string>

namespace myblob::network {

struct HttpRequest {
    enum class Method : uint8_t {
        GET,
        PUT,
        POST,
        DELETE
    };
    
    enum class Type : uint8_t {
        HTTP_1_0,
        HTTP_1_1
    };
    
    std::map<std::string, std::string> queries;
    std::map<std::string, std::string> headers;
    Method method{Method::GET};
    Type type{Type::HTTP_1_1};
    std::string path;
};

}  // namespace myblob::network
```

### 5.2 http_response.hpp - 添加 Code 枚举和辅助方法

> 📁 文件：`include/network/http_response.hpp`（**需要修改**）

**当前问题**：缺少 `Code` 枚举、`deserialize` 和辅助方法

**修改后**：

```cpp
#pragma once
#include <cstdint>
#include <map>
#include <string>
#include <string_view>

namespace myblob::network {

struct HttpResponse {
    enum class Code : uint8_t {
        OK_200,
        CREATED_201,
        NO_CONTENT_204,
        PARTIAL_CONTENT_206,
        BAD_REQUEST_400,
        UNAUTHORIZED_401,
        FORBIDDEN_403,
        NOT_FOUND_404,
        CONFLICT_409,
        LENGTH_REQUIRED_411,
        RANGE_NOT_SATISFIABLE_416,
        TOO_MANY_REQUESTS_429,
        INTERNAL_SERVER_ERROR_500,
        SERVICE_UNAVAILABLE_503,
        SLOW_DOWN_503,
        UNKNOWN = 255
    };
    
    enum class Type : uint8_t {
        HTTP_1_0,
        HTTP_1_1
    };
    
    std::map<std::string, std::string> headers;
    Code code{Code::UNKNOWN};
    Type type{Type::HTTP_1_1};
    
    static constexpr std::string_view getResponseCode(const Code& code) noexcept {
        switch (code) {
            case Code::OK_200: return "200 OK";
            case Code::CREATED_201: return "201 Created";
            case Code::NO_CONTENT_204: return "204 No Content";
            case Code::PARTIAL_CONTENT_206: return "206 Partial Content";
            case Code::BAD_REQUEST_400: return "400 Bad Request";
            case Code::UNAUTHORIZED_401: return "401 Unauthorized";
            case Code::FORBIDDEN_403: return "403 Forbidden";
            case Code::NOT_FOUND_404: return "404 Not Found";
            case Code::CONFLICT_409: return "409 Conflict";
            case Code::LENGTH_REQUIRED_411: return "411 Length Required";
            case Code::RANGE_NOT_SATISFIABLE_416: return "416 Range Not Satisfiable";
            case Code::TOO_MANY_REQUESTS_429: return "429 Too Many Requests";
            case Code::INTERNAL_SERVER_ERROR_500: return "500 Internal Server Error";
            case Code::SERVICE_UNAVAILABLE_503: return "503 Service Unavailable";
            case Code::SLOW_DOWN_503: return "503 Slow Down";
            default: return "UNKNOWN";
        }
    }
    
    static constexpr uint64_t getResponseCodeNumber(const Code& code) noexcept {
        switch (code) {
            case Code::OK_200: return 200;
            case Code::CREATED_201: return 201;
            case Code::NO_CONTENT_204: return 204;
            case Code::PARTIAL_CONTENT_206: return 206;
            case Code::BAD_REQUEST_400: return 400;
            case Code::UNAUTHORIZED_401: return 401;
            case Code::FORBIDDEN_403: return 403;
            case Code::NOT_FOUND_404: return 404;
            case Code::CONFLICT_409: return 409;
            case Code::LENGTH_REQUIRED_411: return 411;
            case Code::RANGE_NOT_SATISFIABLE_416: return 416;
            case Code::TOO_MANY_REQUESTS_429: return 429;
            case Code::INTERNAL_SERVER_ERROR_500: return 500;
            case Code::SERVICE_UNAVAILABLE_503: return 503;
            case Code::SLOW_DOWN_503: return 503;
            default: return 0;
        }
    }
    
    static constexpr auto checkSuccess(const Code& code) {
        return (code == Code::OK_200 || code == Code::CREATED_201 || 
                code == Code::NO_CONTENT_204 || code == Code::PARTIAL_CONTENT_206);
    }
    
    static constexpr auto withoutContent(const Code& code) {
        return code == Code::NO_CONTENT_204;
    }
    
    static constexpr std::string_view getResponseType(const Type& type) noexcept {
        switch (type) {
            case Type::HTTP_1_0: return "HTTP/1.0";
            case Type::HTTP_1_1: return "HTTP/1.1";
            default: return "UNKNOWN";
        }
    }
    
    static HttpResponse deserialize(std::string_view data);
};

}  // namespace myblob::network
```

**新增功能**：

| 方法 | 说明 |
|------|------|
| `getResponseCode()` | 获取状态码字符串 |
| `getResponseCodeNumber()` | 获取状态码数字 |
| `checkSuccess()` | 检查是否 2xx 成功 |
| `withoutContent()` | 检查是否无响应体（如 204） |
| `deserialize()` | 从字符串解析响应 |

### 5.3 http_response.cpp - deserialize 实现

> 📁 文件：`src/network/http_response.cpp`（**新建此文件**）

```cpp
// ============================================================================
// http_response.cpp - HTTP 响应解析实现
// ============================================================================

#include "network/http_response.hpp"
#include "../utils/data_vector.hpp"
#include <map>
#include <stdexcept>
#include <string_view>

namespace myblob::network {

HttpResponse HttpResponse::deserialize(std::string_view data) {
    static constexpr std::string_view strHttp1_0 = "HTTP/1.0";
    static constexpr std::string_view strHttp1_1 = "HTTP/1.1";
    static constexpr std::string_view strNewline = "\r\n";
    static constexpr std::string_view strHeaderSeperator = ": ";

    HttpResponse response;

    std::string_view line;
    bool firstLine = true;

    while (true) {
        auto pos = data.find(strNewline);
        if (pos == data.npos)
            throw std::runtime_error("Invalid HttpResponse: Incomplete header!");

        line = data.substr(0, pos);
        data = data.substr(pos + strNewline.size());

        if (line.empty()) {
            if (!firstLine)
                break;
            else
                throw std::runtime_error("Invalid HttpResponse: Missing first line!");
        }

        if (firstLine) {
            firstLine = false;

            if (line.starts_with(strHttp1_0)) {
                response.type = Type::HTTP_1_0;
            } else if (line.starts_with(strHttp1_1)) {
                response.type = Type::HTTP_1_1;
            } else {
                throw std::runtime_error("Invalid HttpResponse: Needs HTTP/1.0 or HTTP/1.1!");
            }

            std::string_view httpType = getResponseType(response.type);
            line = line.substr(httpType.size() + 1);

            response.code = Code::UNKNOWN;
            for (auto code = static_cast<uint8_t>(Code::OK_200); 
                 code <= static_cast<uint8_t>(Code::SLOW_DOWN_503); 
                 code++) {
                const std::string_view responseCode = getResponseCode(static_cast<Code>(code));
                if (line.starts_with(responseCode)) {
                    response.code = static_cast<Code>(code);
                }
            }
        } else {
            auto keyPos = line.find(strHeaderSeperator);
            std::string_view key, value = "";
            if (keyPos == line.npos) {
                throw std::runtime_error("Invalid HttpResponse: Headers need key and value!");
            } else {
                key = line.substr(0, keyPos);
                value = line.substr(keyPos + strHeaderSeperator.size());
            }
            response.headers.emplace(key, value);
        }
    }

    return response;
}

}  // namespace myblob::network
```

**解析流程**：

```
输入：
"HTTP/1.1 200 OK\r\n"
"Content-Length: 1234\r\n"
"Content-Type: text/html\r\n"
"\r\n"

处理过程：
1. 读取第一行 "HTTP/1.1 200 OK"
   - 确定 HTTP 版本：HTTP/1.1
   - 确定状态码：200

2. 循环读取后续行，直到空行
   - "Content-Length: 1234" → headers["Content-Length"] = "1234"
   - "Content-Type: text/html" → headers["Content-Type"] = "text/html"
   - "" → 遇到空行，结束

输出：
HttpResponse {
    code: 200,
    type: HTTP_1_1,
    headers: {
        "Content-Length": "1234",
        "Content-Type": "text/html"
    }
}
```

### 5.4 provider.hpp - 添加异步方法

> 📁 文件：`include/cloud/provider.hpp`（**需要修改**）

**修改内容**：添加 `getRequest`、`putRequest`、`deleteRequest` 纯虚方法

```cpp
#pragma once
#include "cloud_service.hpp"
#include "../network/http_client.hpp"
#include "../network/connection_manager.hpp"
#include "../utils/data_vector.hpp"
#include <memory>
#include <string>
#include <string_view>
#include <utility>

namespace myblob::cloud {

class Provider {
public:
    static constexpr const char* remoteFile[] = {
        "https://",
        "http://",
        "s3://",
        "azure://",
        "gs://",
        "oci://",
        "ibm://",
        "minio://"
    };
    
    static constexpr size_t remoteFileCount = 8;
    
    explicit Provider(network::ConnectionManager& conn_mgr,
                      network::HttpClient& http_client,
                      CloudService type = CloudService::HTTPS);
    
    Provider(const std::string& addr, uint16_t port,
             network::ConnectionManager& conn_mgr,
             network::HttpClient& http_client,
             CloudService type);
    
    virtual ~Provider() = default;
    
    static std::unique_ptr<Provider> makeProvider(
        const std::string& url,
        bool https = false,
        const std::string& access_key = "",
        const std::string& secret_key = "",
        void* send_receiver_handle = nullptr
    );
    
    static bool isRemoteFile(const std::string& url);
    
    static CloudService getCloudService(const std::string& url);
    
    virtual std::unique_ptr<utils::DataVector<uint8_t>> getRequest(
        const std::string& filePath,
        const std::pair<uint64_t, uint64_t>& range = {0, 0}
    ) const = 0;
    
    virtual std::unique_ptr<utils::DataVector<uint8_t>> putRequest(
        const std::string& filePath,
        std::string_view object
    ) const = 0;
    
    virtual std::unique_ptr<utils::DataVector<uint8_t>> deleteRequest(
        const std::string& filePath
    ) const = 0;
    
    virtual std::string getAddress() const { return address_; }
    
    virtual uint16_t getPort() const { return port_; }
    
    virtual CloudService getType() const { return type_; }
    
    bool isHTTPS() const { return type_ == CloudService::HTTPS; }
    
    network::HttpResponse download(
        const std::string& file_path,
        uint64_t offset = 0,
        uint64_t length = 0
    );
    
protected:
    network::ConnectionManager* conn_mgr_{nullptr};
    network::HttpClient* http_client_{nullptr};
    std::string address_;
    uint16_t port_;
    CloudService type_;
};

}  // namespace myblob::cloud
```

### 5.5 http_provider.hpp - 实现异步方法

> 📁 文件：`include/cloud/http_provider.hpp`（**需要修改**）

```cpp
#pragma once
#include "cloud_service.hpp"
#include "../network/http_client.hpp"
#include "../utils/data_vector.hpp"
#include <memory>
#include <string>
#include <string_view>
#include <utility>
#include "provider.hpp"

namespace myblob::cloud {

class HTTPProvider : public Provider {
public:
    HTTPProvider(const std::string& addr, uint16_t port, bool https,
                 network::ConnectionManager& conn_mgr,
                 network::HttpClient& http_client);
    
    std::unique_ptr<utils::DataVector<uint8_t>> getRequest(
        const std::string& filePath,
        const std::pair<uint64_t, uint64_t>& range = {0, 0}
    ) const override;
    
    std::unique_ptr<utils::DataVector<uint8_t>> putRequest(
        const std::string& filePath,
        std::string_view object
    ) const override;
    
    std::unique_ptr<utils::DataVector<uint8_t>> deleteRequest(
        const std::string& filePath
    ) const override;
    
    std::unique_ptr<utils::DataVector<uint8_t>> createGetRequest(
        const std::string& file_path,
        uint64_t offset = 0,
        uint64_t length = 0
    ) override;
    
    std::string getAddress() const override { return address_; }
    
    uint16_t getPort() const override { return port_; }
    
    CloudService getType() const override { return type_; }

};

}  // namespace myblob::cloud
```

---

## 💻 六、实现文件

### 6.1 http_provider.cpp - 实现异步请求

> 📁 文件：`src/cloud/http_provider.cpp`（**需要修改**）

```cpp
#include "cloud/http_provider.hpp"
#include <iostream>
#include <sstream>

namespace myblob::cloud {

HTTPProvider::HTTPProvider(const std::string& addr, uint16_t port, bool https,
                         network::ConnectionManager& conn_mgr,
                         network::HttpClient& http_client)
    : Provider(addr, port, conn_mgr, http_client,
               https ? CloudService::HTTPS : CloudService::HTTP) {
}

std::unique_ptr<utils::DataVector<uint8_t>> HTTPProvider::getRequest(
    const std::string& filePath,
    const std::pair<uint64_t, uint64_t>& range
) const {
    auto request = std::make_unique<utils::DataVector<uint8_t>>();
    
    std::string request_str = "GET " + filePath + " HTTP/1.1\r\n";
    request_str += "Host: " + address_ + "\r\n";
    request_str += "Connection: close\r\n";
    
    if (range.first != 0 || range.second != 0) {
        std::string range_header = "bytes=" + std::to_string(range.first) + "-";
        if (range.second > 0) {
            range_header += std::to_string(range.second - 1);
        }
        request_str += "Range: " + range_header + "\r\n";
    }
    
    request_str += "\r\n";
    
    request->resize(request_str.size());
    memcpy(request->data(), request_str.c_str(), request_str.size());
    
    return request;
}

std::unique_ptr<utils::DataVector<uint8_t>> HTTPProvider::putRequest(
    const std::string& filePath,
    std::string_view object
) const {
    auto request = std::make_unique<utils::DataVector<uint8_t>>();
    
    std::string request_str = "PUT " + filePath + " HTTP/1.1\r\n";
    request_str += "Host: " + address_ + "\r\n";
    request_str += "Content-Length: " + std::to_string(object.size()) + "\r\n";
    request_str += "Connection: close\r\n";
    request_str += "\r\n";
    
    size_t header_size = request_str.size();
    request->resize(header_size + object.size());
    
    memcpy(request->data(), request_str.c_str(), header_size);
    memcpy(request->data() + header_size, object.data(), object.size());
    
    return request;
}

std::unique_ptr<utils::DataVector<uint8_t>> HTTPProvider::deleteRequest(
    const std::string& filePath
) const {
    auto request = std::make_unique<utils::DataVector<uint8_t>>();
    
    std::string request_str = "DELETE " + filePath + " HTTP/1.1\r\n";
    request_str += "Host: " + address_ + "\r\n";
    request_str += "Connection: close\r\n";
    request_str += "\r\n";
    
    request->resize(request_str.size());
    memcpy(request->data(), request_str.c_str(), request_str.size());
    
    return request;
}

std::unique_ptr<utils::DataVector<uint8_t>> HTTPProvider::createGetRequest(
    const std::string& file_path,
    uint64_t offset,
    uint64_t length
) {
    return getRequest(file_path, {offset, length});
}

}  // namespace myblob::cloud
```

### 6.2 message_result.cpp - 结果实现

> 📁 文件：`src/network/message_result.cpp`（**新建此文件**）

```cpp
#include "network/message_result.hpp"
#include "network/http_helper.hpp"
#include <cstring>

namespace myblob::network {

MessageResult::MessageResult()
    : state_(MessageState::Init) {
}

MessageResult::MessageResult(uint8_t* data, uint64_t size)
    : dataVector_(std::make_unique<utils::DataVector<uint8_t>>(data, size)),
      state_(MessageState::Init) {
}

MessageResult::MessageResult(utils::DataVector<uint8_t>* dataVector)
    : dataVector_(dataVector),
      state_(MessageState::Init) {
}

std::string_view MessageResult::getResult() const {
    if (!dataVector_ || dataVector_->size() == 0) {
        return {};
    }
    return std::string_view(
        reinterpret_cast<const char*>(dataVector_->cdata()),
        dataVector_->size()
    );
}

const uint8_t* MessageResult::getData() const {
    return dataVector_ ? dataVector_->cdata() : nullptr;
}

uint8_t* MessageResult::getData() {
    return dataVector_ ? dataVector_->data() : nullptr;
}

uint64_t MessageResult::getSize() const {
    return response_ ? response_->length : 0;
}

uint64_t MessageResult::getOffset() const {
    return response_ ? response_->headerLength : 0;
}

MessageState MessageResult::getState() const {
    return state_.load();
}

uint16_t MessageResult::getFailureCode() const {
    return failureCode_;
}

bool MessageResult::success() const {
    return state_.load() == MessageState::Finished &&
           failureCode_ == 0;
}

utils::DataVector<uint8_t>& MessageResult::getDataVector() {
    static utils::DataVector<uint8_t> empty;
    return dataVector_ ? *dataVector_ : empty;
}

std::unique_ptr<utils::DataVector<uint8_t>> MessageResult::moveDataVector() {
    return std::move(dataVector_);
}

std::unique_ptr<uint8_t[]> MessageResult::moveData() {
    return dataVector_ ? dataVector_->transferBuffer() : nullptr;
}

std::string_view MessageResult::getErrorResponse() const {
    if (response_)
        return std::string_view(
            reinterpret_cast<const char*>(dataVector_->data()),
            dataVector_->size()
        );
    return {};
}

std::string_view MessageResult::getResponseCode() const {
    if (response_)
        return HttpResponse::getResponseCode(response_->response.code);
    return {};
}

uint64_t MessageResult::getResponseCodeNumber() const {
    if (response_)
        return HttpResponse::getResponseCodeNumber(response_->response.code);
    return 0;
}

bool MessageResult::owned() const {
    return dataVector_ && dataVector_->owned();
}

}  // namespace myblob::network
```

### 6.3 original_message.cpp - 原始消息实现

> 📁 文件：`src/network/original_message.cpp`（**新建此文件**）

```cpp
#include "network/original_message.hpp"
#include "cloud/provider.hpp"

namespace myblob::network {

OriginalMessage::OriginalMessage(
    std::unique_ptr<utils::DataVector<uint8_t>> msg,
    cloud::Provider& provider,
    uint8_t* receiveBuffer,
    uint64_t bufferSize,
    uint64_t trace
) : message(std::move(msg))
  , provider(provider)
  , result(receiveBuffer, bufferSize)
  , putData(nullptr)
  , putLength()
  , traceId(trace) {
}

}  // namespace myblob::network
```

### 6.4 transaction.cpp - 事务实现

> 📁 文件：`src/cloud/transaction.cpp`（**新建此文件**）

```cpp
#include "cloud/transaction.hpp"

namespace myblob::cloud {

Transaction::Transaction()
    : provider_(nullptr) {
}

Transaction::Transaction(Provider* provider)
    : provider_(provider) {
}

void Transaction::setProvider(Provider* provider) {
    provider_ = provider;
}

bool Transaction::getObjectRequest(
    const std::string& remotePath,
    std::pair<uint64_t, uint64_t> range,
    uint8_t* result,
    uint64_t capacity
) {
    if (!provider_) return false;
    
    auto msg = std::make_unique<network::OriginalMessage>(
        provider_->getRequest(remotePath, range),
        *provider_,
        result,
        capacity
    );
    
    messages_.push_back(std::move(msg));
    return true;
}

bool Transaction::putObjectRequest(
    const std::string& remotePath,
    const char* data,
    uint64_t size,
    uint8_t* result,
    uint64_t capacity
) {
    if (!provider_) return false;
    
    auto object = std::string_view(data, size);
    auto msg = std::make_unique<network::OriginalMessage>(
        provider_->putRequest(remotePath, object),
        *provider_,
        result,
        capacity
    );
    
    msg->setPutRequestData(reinterpret_cast<const uint8_t*>(data), size);
    messages_.push_back(std::move(msg));
    return true;
}

bool Transaction::deleteObjectRequest(
    const std::string& remotePath,
    uint8_t* result,
    uint64_t capacity
) {
    if (!provider_) return false;
    
    auto msg = std::make_unique<network::OriginalMessage>(
        provider_->deleteRequest(remotePath),
        *provider_,
        result,
        capacity
    );
    
    messages_.push_back(std::move(msg));
    return true;
}

}  // namespace myblob::cloud
```

---

## 🧪 七、测试示例

### 7.1 同步下载

```cpp
#include "cloud/provider.hpp"
#include "cloud/transaction.hpp"
#include <iostream>

int main() {
    auto provider = Provider::makeProvider("http://example.com/file.txt");
    if (!provider) return 1;
    
    Transaction transaction(provider.get());
    
    uint8_t buffer[1024];
    if (transaction.getObjectRequest("/data/file.txt", {0, 0}, buffer, sizeof(buffer))) {
        std::cout << "请求已添加到事务" << std::endl;
        
        for (auto& msg : transaction.getMessages()) {
            auto& result = msg->result;
            
            std::cout << "状态: " << static_cast<int>(result.getState()) << std::endl;
            std::cout << "成功: " << (result.success() ? "是" : "否") << std::endl;
            
            if (result.success()) {
                std::cout << "大小: " << result.getSize() << std::endl;
            }
        }
    }
    
    return 0;
}
```

### 7.2 异步回调下载

```cpp
#include "cloud/provider.hpp"
#include "cloud/transaction.hpp"
#include <functional>

void onDownloadComplete(network::MessageResult& result) {
    if (result.success()) {
        std::cout << "下载成功! 大小: " << result.getSize() << std::endl;
    } else {
        std::cout << "下载失败! 状态: " << static_cast<int>(result.getState()) << std::endl;
    }
}

int main() {
    auto provider = Provider::makeProvider("http://example.com/file.txt");
    if (!provider) return 1;
    
    Transaction transaction(provider.get());
    
    transaction.getObjectRequest(onDownloadComplete, "/data/file.txt");
    
    std::cout << "请求已添加，异步处理中..." << std::endl;
    
    return 0;
}
```

---

## 📊 八、编译配置

### 8.1 更新 CMakeLists.txt

```cmake
cmake_minimum_required(VERSION 3.10)
project(MyBlob)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

include_directories(${CMAKE_SOURCE_DIR}/include)

find_package(OpenSSL REQUIRED)

set(SOURCES
    src/cloud/provider.cpp
    src/cloud/http_provider.cpp
    src/cloud/transaction.cpp
    src/network/http_client.cpp
    src/network/connection.cpp
    src/network/connection_mannager.cpp
    src/network/http_request.cpp
    src/network/http_response.cpp
    src/network/message_result.cpp
)

add_executable(simple_download example/example.cpp ${SOURCES})

target_link_libraries(simple_download OpenSSL::SSL OpenSSL::Crypto pthread)
```

### 8.2 编译命令

```bash
cd /home/root1/myblob
mkdir -p build
cd build
cmake ..
make
```

---

## ✅ 九、待办事项

### 已完成
- [x] 定义 MessageState 枚举
- [x] 定义 MessageResult 类
- [x] 定义 OriginalMessage 结构
- [x] 定义 Transaction 类
- [x] 更新 HttpRequest 添加 Method 枚举
- [x] 更新 HttpResponse 添加 Code 枚举
- [x] 更新 Provider 添加异步方法
- [x] 实现 HTTPProvider 异步方法

### 待实现（后续版本）
- [ ] 实现 TaskedSendReceiver（任务调度器）
- [ ] 实现多线程并发处理
- [ ] 实现大文件分片上传
- [ ] 实现 epoll/io_uring 多路复用

---

## 📚 十、参考文档

- AnyBlob 源码：`/home/root1/AnyBlob/include/network/message_result.hpp`
- AnyBlob 源码：`/home/root1/AnyBlob/include/network/original_message.hpp`
- AnyBlob 源码：`/home/root1/AnyBlob/include/network/transaction.hpp`
- 学习路线图：`学习路线图.md`
- 代码审查清单：`代码审查清单.md`

---

**最后更新**: 2026-04-02
**版本**: 4.0
**维护者**: AI Assistant
