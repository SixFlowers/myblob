# 编译错误修复记录

## 修复日期
2026-04-09

---

## 错误 1: `_state._internalBioWrite` 拼写错误

**位置**: `src/network/tls_connection.cpp` 第 137 行

**错误代码**:
```cpp
_state._internalBioWrite = BIO_ctrl_pending(_networkBio);
```

**问题**: 成员变量是 `internalBioWrite`，不是 `_internalBioWrite`

**修复**:
```cpp
_state.internalBioWrite = BIO_ctrl_pending(_networkBio);
```

---

## 错误 2: `sattic_cast` 拼写错误

**位置**: `src/network/tls_connection.cpp` 第 151 行

**错误代码**:
```cpp
_state.socketWrite += sattic_cast<uint64_t>(_message->request->length);
```

**问题**: 应该是 `static_cast`

**修复**:
```cpp
_state.socketWrite += static_cast<uint64_t>(_message->request->length);
```

---

## 错误 3: `writeSize` 类型错误

**位置**: `src/network/tls_connection.cpp` 第 161 行

**错误代码**:
```cpp
auto writeSize = static_cast<uint8_t*>(_buffer.get()) + _state.socketWrite;
```

**问题**: `writeSize` 应该是 `size_t` 类型表示字节数，而不是指针

**修复**:
```cpp
auto writeSize = static_cast<size_t>(_state.networkBioRead) - _state.socketWrite;
```

---

## 错误 4: `.lengt` 拼写错误

**位置**: `src/network/tls_connection.cpp` 第 166 行

**错误代码**:
```cpp
.lengt = static_cast<int64_t>(writeSize),
```

**问题**: 字段名拼写错误，应该是 `length`

**修复**:
```cpp
.length = static_cast<int64_t>(writeSize),
```

---

## 错误 5: `_state.progress == Progress::Aborted` 赋值变比较

**位置**: `src/network/tls_connection.cpp` 第 153 行

**错误代码**:
```cpp
_state.progress == Progress::Aborted;
```

**问题**: 使用 `==` 比较而不是 `=` 赋值

**修复**:
```cpp
_state.progress = Progress::Aborted;
```

---

## 错误 6: `getPollSocket` 方法调用缺少括号

**位置**: `src/network/tls_connection.cpp` 第 172 行

**错误代码**:
```cpp
connectionManager.getPollSocket.send_to(*_message->request, _message->tcpSettings.timeout);
```

**问题**: `getPollSocket()` 是一个方法，需要加括号

**修复**:
```cpp
connectionManager.getPollSocket().send_to(*_message->request, _message->tcpSettings.timeout);
```

---

## 错误 7: `_state.process` 应为 `_state.progress`

**位置**: `src/network/tls_connection.cpp` 第 148 行

**错误代码**:
```cpp
if(_state.process == Progress::Sending){
```

**问题**: 状态成员是 `progress` 不是 `process`

**修复**:
```cpp
if (_state.progress == Progress::Sending) {
```

---

## 错误 8: `SSL_set_contect_state` 拼写错误

**位置**: `src/network/tls_connection.cpp` 第 36 行

**错误代码**:
```cpp
SSL_set_contect_state(_ssl);
```

**问题**: 函数名拼写错误，应该是 `SSL_set_connect_state`

**修复**:
```cpp
SSL_set_connect_state(_ssl);
```

---

## 错误 9: `_netwrokBio` 拼写错误

**位置**: `src/network/tls_connection.cpp` 第 209 行

**错误代码**:
```cpp
_state.internalBioRead = BIO_ctrl_get_read_request(_netwrokBio);
```

**问题**: 变量名拼写错误，应该是 `_networkBio`

**修复**:
```cpp
_state.internalBioRead = BIO_ctrl_get_read_request(_networkBio);
```

---

## 错误 10: `netwrokBioWrite` 拼写错误

**位置**: `src/network/tls_connection.cpp` 多处

**错误代码**:
```cpp
_state.netwrokBioWrite += BIO_write(
    _netwrokBio,
```

**问题**: 变量名拼写错误，应该是 `networkBioWrite` 和 `_networkBio`

**修复**:
```cpp
_state.networkBioWrite += BIO_write(
    _networkBio,
```

---

## 错误 11: `_message->reqeust` 拼写错误

**位置**: `src/network/tls_connection.cpp` 第 215 行

**错误代码**:
```cpp
if(_message->reqeust && _message->request->length == 0){
```

**问题**: 变量名拼写错误，应该是 `request`

**修复**:
```cpp
if (_message->request && _message->request->length == 0) {
```

---

## 错误 12: `request->lengt` 拼写错误

**位置**: `src/network/tls_connection.cpp` 第 220 行

**错误代码**:
```cpp
assert(_message->request->lengt <= INT_MAX);
```

**问题**: 字段名拼写错误，应该是 `length`

**修复**:
```cpp
assert(_message->request->length <= INT_MAX);
```

---

## 错误 13: `.recvData` 应为 `.data`

**位置**: `src/network/tls_connection.cpp` 第 245 行

**错误代码**:
```cpp
.data = {.recvData = ptr},
```

**问题**: Socket::Request 的 union 字段应该用 `data`

**修复**:
```cpp
.data = {.data = ptr},
```

---

## 错误 14: `ptr` 类型错误

**位置**: `src/network/tls_connection.cpp` 第 241 行

**错误代码**:
```cpp
uint8_t ptr = reinterpret_cast<uint8_t*>(_buffer.get()) + _state.socketRead;
```

**问题**: `ptr` 应该是指针类型 `uint8_t*`

**修复**:
```cpp
uint8_t* ptr = reinterpret_cast<uint8_t*>(_buffer.get()) + _state.socketRead;
```

---

## 错误 15: `operationHelper` 中 `_state.process` 应为 `_state.progress`

**位置**: `src/network/tls_connection.cpp` 第 68 行

**错误代码**:
```cpp
if(_state.process == Progress::Finished || _state.progress == Progress::Init){
```

**问题**: 第一个条件用了 `process` 而不是 `progress`

**修复**:
```cpp
if (_state.progress == Progress::Finished || _state.progress == Progress::Init) {
```

---

## 错误 16: `RecivingInit` 拼写错误

**位置**: `src/network/tls_connection.cpp` 第 181 行

**错误代码**:
```cpp
_state.progress = Progress::RecivingInit;
```

**问题**: 应该是 `ReceivingInit`

**修复**:
```cpp
_state.progress = Progress::ReceivingInit;
```

---

## 错误 17: `getSize()` 永远返回 0（response_ 未设置）

**位置**: `src/network/message_result.cpp` 第 31-33 行

**错误代码**:
```cpp
uint64_t MessageResult::getSize() const {
    return response_ ? response_->length : 0;
}
```

**问题**: `response_` 指针从未被设置（代码中缺少调用 `HttpHelper::finished()` 解析 HTTP 响应），导致永远返回 0

**修复**:
```cpp
uint64_t MessageResult::getSize() const {
    if (dataVector_) {
        return dataVector_->size();
    }
    return response_ ? response_->length : 0;
}
```

---

## 错误 18: `getOffset()` 永远返回 0（response_ 未设置）

**位置**: `src/network/message_result.cpp` 第 35-37 行

**错误代码**:
```cpp
uint64_t MessageResult::getOffset() const {
    return response_ ? response_->headerLength : 0;
}
```

**问题**: `response_` 指针从未被设置，导致永远返回 0

**修复**:
```cpp
uint64_t MessageResult::getOffset() const {
    if (dataVector_ && dataVector_->size() > 4) {
        std::string_view sv(reinterpret_cast<const char*>(dataVector_->cdata()), dataVector_->size());
        auto pos = sv.find("\r\n\r\n");
        if (pos != std::string_view::npos) {
            return pos + 4;
        }
    }
    return response_ ? response_->headerLength : 0;
}
```

---

## 总结

| 类型 | 数量 |
|------|------|
| 拼写错误 (static_cast) | 1 |
| 拼写错误 (变量名) | 5 |
| 拼写错误 (方法名) | 2 |
| 拼写错误 (字段名) | 2 |
| 赋值变比较 | 1 |
| 类型错误 | 2 |
| 缺少括号 | 1 |
| union 字段名错误 | 1 |
| 状态名拼写错误 | 1 |
| response_ 未设置（运行时） | 2 |

**总计**: 18 个错误（16 个编译错误 + 2 个运行时问题）

所有编译错误已修复，代码现已编译通过。
